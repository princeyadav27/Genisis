# Verification evidence
## Runtime
Linux x86_64 sandbox, Node v22.22.1, Next.js 16.3.4, PostgreSQL via Drizzle 0.45.2, React 19.2.6, Chromium via Playwright 1.63.0. Seed 42; 20 residents; mock planner; no provider keys. No hardware throughput benchmark is claimed.

## Executed checks
- `npx drizzle-kit push` — schema applied successfully after managed environment bootstrap. Equivalent fresh-schema SQL checked in at migrations/0001_genesis.sql.
- `npx tsx --test tests/simulation.test.ts` — **5 passed**: 1000-tick deterministic continuation, nonnegative integer inventory, max carrying capacity 5, max movement 1.5/tick, conserved 48,000 currency minor units, construction material/labor requirement, stable pause, duplicate/stale-generation rejection, checkpoint continuation, finite extraction and source/sink reconciliation.
- Final seeded 1000-tick observed result: food 797, water 1061, stored wood 754; completed construction with 30 wood, 18 stone, 60 labor; 20,466 events. Ending maximum hunger 55.90 and thirst 73.95. This is a resource baseline test, not proof of health/mortality/seasonal sustainability.
- `BASE_URL=http://127.0.0.1:3001 npx playwright test` — **3 passed** on temporary debug server after renderer fixes. API serial writes, save/load ongoing state, stable pause hash, concurrent ticks, malformed command rejection; live resident movement, start/pause/step/save/load, follow, zoom/reset, exact inverse tile picking at .88 and 1.03 zoom, layers, project/economy/agent panels, event inspector; conversation persistence and grounded outcome. Browser asserts zero page errors and zero console errors.
- `npm run lint` — passed.
- `bash -lc 'set -o pipefail; npx next typegen 2>&1 | tee /tmp/next-typegen.log'` — passed.
- `bash -lc 'set -o pipefail; npm exec tsc -- --noEmit --pretty false 2>&1 | tee /tmp/tsc.log'` — passed.
- `bash -lc 'set -o pipefail; npm run build 2>&1 | tee /tmp/build.log'` — passed.
- Managed `build_and_start` — production build/start and /api/health passed on prior validation rounds. Final production rerun is recorded below after the final changes.
- `npm audit --omit=dev --json` — starter had critical/high Next.js issues; Next.js/eslint-config-next updated to 16.3.4. Current production tree has 0 critical, 0 high, 1 moderate advisory (`baseline-browser-mapping`).

## Visual evidence
Screenshots generated by real Chromium automation:
- artifacts/overview-1366.png — overview at 1366×768.
- artifacts/resident-inspection-1366.png — follow and selected resident zoom.
- artifacts/overview-1920.png — overview at 1920×1080.
- artifacts/street-1920.png — closer village view.
- artifacts/conversation-1366.png — grounded conversation inspector.
Read and inspected overview screenshots during development. No attached visual reference was accessible; no pixel-perfect claim. Screenshot artifacts include test-state checkpoints, not fictitious activities.

## Failures investigated, not hidden
1. Parallel file auto-typechecks exhausted resources; terminated overlapping stale checks and ran sequentially.
2. Renderer scope edit race; consolidated edits and fixed missing farmLabor declaration.
3. Lint home anchor violation; replaced with Next Link.
4. Managed runtime healthcheck timeout from stale startup log; inspected logs, removed stale file and reran successfully.
5. Chromium missing libnspr4; installed Playwright system dependencies.
6. Exact status text assertions exposed unwrapped text nodes; added semantic status spans.
7. Server/browser Math.sin float differences, multi-child SVG title SSR omission and invalid canopy path; replaced noise with integer hash, composed single title string and corrected path. No hydration suppression used.

## Final production result
After the final command-race fix, ran the complete engine/lint/type-generation/TypeScript/production-build sequence successfully. Managed `build_and_start` passed (production startup and PostgreSQL healthcheck).

`BASE_URL=http://127.0.0.1:3000 npx playwright test` — **3 passed in 9.2 seconds** against the final production server. All five screenshot artifacts were regenerated from that production build. `curl -fsS http://127.0.0.1:3000/api/health` returned `{"ok":true}`. The temporary debugging server on port 3001 was stopped. Test cleanup reset the local world to seed 42, paused.

These timings describe this test run, not simulation scale benchmarks. No release-completion marker is emitted.

## Not verified
Full Release A acceptance, general obstacle/pathfinding, negotiated jobs/trades, LLM adapter, all supervisor contracts, save corruption import, full reconnect workflow, Docker build, mobile accessibility, detailed occlusion walk-through, 50/100 population throughput, multiple divergent seeds, Release B or C. See BLOCKERS.md.
